import SwiftUI

struct AppColors {
    static let background = Color("Background")
    static let primary = Color.orange
    static let secondary = Color(red: 1.0, green: 0.7, blue: 0.3)
    static let accent = Color(red: 1.0, green: 0.5, blue: 0.0)
    static let text = Color(red: 0.2, green: 0.1, blue: 0.05)
    static let card = Color(red: 1.0, green: 0.95, blue: 0.9)
    static let tabBar = Color(red: 1.0, green: 0.85, blue: 0.7)
    static let buttonText = Color.white
    static let buttonBackground = Color.orange
} 